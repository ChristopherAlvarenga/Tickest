@model Tickest.Models.ViewModels.TicketViewModel

<div>
    <h1>Detalhes do Ticket</h1>
    <dl class="row">
        <dt class="col-sm-2">ID:</dt>
        <dd class="col-sm-10">@Model.Ticket.Id</dd>

        <dt class="col-sm-2">Título:</dt>
        <dd class="col-sm-10">@Model.Ticket.Titulo</dd>

        <dt class="col-sm-2">Descrição:</dt>
        <dd class="col-sm-10">@Model.Ticket.Descricao</dd>

        <dt class="col-sm-2">Departamento:</dt>
        <dd class="col-sm-10">@Model.Ticket.Departamento.Nome</dd>

        <!-- Adicione mais detalhes conforme necessário -->
    </dl>
</div>
